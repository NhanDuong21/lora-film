# ===== Database Connection for Test Profile =====
spring.datasource.url=jdbc:mysql://${DB_HOST:localhost}:${DB_PORT:${MYSQL_PORT:3307}}/${MYSQL_DATABASE:notification_db}?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
spring.datasource.username=${MYSQL_USER:movieuser}
spring.datasource.password=${MYSQL_PASSWORD:THGaming809}

# ===== Kafka Configuration for Test Profile =====
spring.kafka.bootstrap-servers=${KAFKA_BOOTSTRAP_SERVERS:localhost:9092}

# ===== Mail Configuration for Test Profile =====
spring.mail.host=localhost
spring.mail.port=1025
spring.mail.username=test
spring.mail.password=test

# ===== JWT Configuration for Test Profile =====
jwt.secret=${JWT_SECRET_KEY:dGVzdF9vbmx5X3NlY3JldF9rZXlfdGhhdF9pc19hdF9sZWFzdF8zMl9ieXRlc19sb25n}

# ===== Notification Provider override for Test Profile =====
notification.email.provider=mock

# ===== Eureka Instance Config (Fix Windows DNS resolution issue) =====
eureka.instance.prefer-ip-address=true
eureka.instance.ip-address=127.0.0.1
eureka.instance.hostname=localhost
