spring.datasource.url=jdbc:h2:mem:booking_db;DB_CLOSE_DELAY=-1;MODE=MySQL
spring.datasource.driver-class-name=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=
spring.jpa.database-platform=org.hibernate.dialect.H2Dialect
spring.jpa.hibernate.ddl-auto=update

jwt.secret=404E635266556A586E3272357538782F413F4428472B4B6250645367566B5970
jwt.expiration=86400000
app.internal-token=secret-internal-token
app.internal-payment-token=payment-test-token
booking.max-seats-per-booking=8
booking.hold-duration-seconds=900
booking.creation-lock-ttl-seconds=30
booking.realtime.enabled=false
booking.kafka.promotion-consumer-enabled=false
# Retained only for deprecated standalone reservation compatibility.
booking.reservation-timeout=300

# ===== Eureka Instance Config (Fix Windows DNS resolution issue) =====
eureka.client.enabled=false
eureka.instance.prefer-ip-address=true
eureka.instance.ip-address=127.0.0.1
eureka.instance.hostname=localhost
