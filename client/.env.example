# Leave empty when the client and Gateway share an origin or Vite proxy is used.
VITE_API_BASE_URL=
# Optional direct Socket.IO endpoint; omit to use the Gateway /socket.io route.
# VITE_BOOKING_SOCKET_URL=http://localhost:8080
VITE_USE_AUTH_MOCK=false
